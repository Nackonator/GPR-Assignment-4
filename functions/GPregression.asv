function[pred_mean, pred_cov] = GPregression(X,y,Xstar,hyp)

    N = dim(X,1);

    GPmodel = fitrgp(X,y,'KernelFunction','squaredexponential','KernelParameters',hyp);
    [l,sign] = GPmodel.KernelInformation.KernelParameters;

    K_matrix = zeros(N,N);

    % for i = 1:N
    %     for j =i+1:N
    % 
    %     end
    % end

    X_perm = permute(X,);

end